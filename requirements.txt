# general  modules
django>5.2,<5.3
Pillow
djangorestframework

# database client
psycopg[binary,pool]

# ── Production WSGI server ───────────────────────────────────────
gunicorn
 
# ── Static files (served efficiently without Nginx needing app access) ──
whitenoise

# ── Rate limiting for download endpoints (see 10_EXPLANATIONS.md) ─
django-ratelimit

# ── Environment variable loading ──────────────────────────────────
python-dotenv

# ── Optional, only if you outgrow local disk storage ─────────────
# django-storages[s3]==1.14.*
# boto3==1.34.*

# env controlls
python-decouple

# third party modules
markdown       
django-filter  
drf-spectacular
djangorestframework-simplejwt
django-cors-headers
django-robots
django-debug-toolbar
drf-yasg[validation]
djoser
django-hitcount
python-slugify
bleach

# refactoring and tests
black
flake8
pytest
pytest-django
Faker

# background proccess and cache
celery
django-redis


